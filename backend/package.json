{
  "name": "jose-oviedo-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/index.mjs",
    "start": "node --env-file-if-exists=.env src/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "deploy:supabase": "supabase functions deploy api --project-ref oiipswlnxbcgfflqnlid --use-api"
  },
  "dependencies": {
    "fast-xml-parser": "^5.11.1"
  },
  "devDependencies": {
    "supabase": "^2.117.0"
  }
}
