# Vite usa este destino únicamente en desarrollo y preview.
BACKEND_URL=http://127.0.0.1:3000
# En producción, vacío si el servidor redirige /api al backend.
# Si se publica en otro dominio, usar su URL SIN /api y configurar CORS_ORIGINS allí.
# Las variables VITE_ son públicas; nunca colocar contraseñas ni claves.
VITE_API_URL=
